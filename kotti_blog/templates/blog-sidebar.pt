Categories:

<ul>
  <li tal:repeat="tag unique_tags">
    <a href="${blog_url}?selected-tag=${tag}">${tag}</a>
  </li>
</ul>


Dates:

<ul>
  <li tal:repeat="date dates">
    <a href="${blog_url}?selected-date=${date.year}_${date.month}">${api.format_date(date, 'MMMM yyyy')}</a>
  </li>
</ul>

